export enum HAIKU_TYPES  {
    // Make the poem look like it is a letter
    LETTER,
    // Format the poem so it has a title, and footnotes
    TITLE_FOOTNOTES,
    // Allow for lines of poem to change
    CHANGING,
}